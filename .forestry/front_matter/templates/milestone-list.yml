---
label: MILESTONE LIST
hide_body: false
fields:
- name: milestones
  type: field_group_list
  fields:
  - name: tasks
    type: include
    config: {}
    template: task
    label: Tasks
    description: All tasks in the current milestone
  config:
    min: 
    max: 
    labelField: 
  label: Milestones
  description: all milestones across all experience types
pages:
- content.json
