---
label: MILESTONE
hide_body: true
display_field: label
fields:
- name: id
  type: text
  config:
    required: false
  label: ID
  description: must match the ID in our database
- name: tasks
  type: field_group_list
  fields:
  - name: task
    type: include
    config: {}
    template: task
    label: Task
    description: uhh
  config:
    min: 
    max: 
    labelField: forestryLabel
  label: Milestone Tasks
  description: Tasks within the current milestone
